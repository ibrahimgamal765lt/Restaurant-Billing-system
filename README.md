# Order Management System
A Python-based GUI application built with Tkinter that simplifies the restaurant ordering process. Inspired by pop culture, this app allows users to browse a digital menu, build a custom order in real-time, and instantly generate a formatted receipt with total price calculations.
